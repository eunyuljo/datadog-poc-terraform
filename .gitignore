# -----------------------------------------------------------------------------
# Terraform
# -----------------------------------------------------------------------------
# 모듈 캐시 (하위 어디에 있든 무시)
**/.terraform/*

# 상태 파일 및 백업
*.tfstate
*.tfstate.*

# Plan 산출물
*.tfplan

# 크래시 로그
crash.log
crash.*.log

# 시크릿 포함 가능 - 절대 커밋 금지
# (datadog_external_id 등이 들어감. .example 만 커밋)
terraform.tfvars
*.auto.tfvars
*.tfvars.json

# 로컬 오버라이드
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# 참고: .terraform.lock.hcl 은 커밋 권장 (provider 버전 재현성).
#      의도적으로 무시하지 않는다.

# -----------------------------------------------------------------------------
# 환경 변수 / 시크릿
# -----------------------------------------------------------------------------
.env
.env.local
.env.*.local
*.pem
*.key

# -----------------------------------------------------------------------------
# 에디터 / IDE
# -----------------------------------------------------------------------------
.idea/
.vscode/
*.swp
*.swo
*~
.~lock.*

# -----------------------------------------------------------------------------
# OS
# -----------------------------------------------------------------------------
.DS_Store
Thumbs.db
Desktop.ini

# -----------------------------------------------------------------------------
# Obsidian (README/노트가 Obsidian 형식이지만 workspace 상태는 개인 로컬)
# -----------------------------------------------------------------------------
.obsidian/
.trash/

# -----------------------------------------------------------------------------
# 백업 / 임시
# -----------------------------------------------------------------------------
*.bak
*.orig
*.rej
