---
tags: [datadog, poc, positioning, roadmap, partnership, co-design, preview]
type: reference
status: draft
created: 2026-08-11
aliases:
  - "이 PoC 의 성격 — Preview 와 자동화 확장, 그리고 파트너십"
  - "PoC 포지셔닝"
related:
  - "[[Datadog PoC — 진행 방향 및 Terraform 준비]]"
  - "[[Datadog AI 자율운영 스택 — 3개 기능의 원래 목적]]"
  - "[[메가존 PoC 실행 계획]]"
---

# 이 PoC 의 성격 — Preview 와 자동화 확장, 그리고 파트너십

> [!info] 한 줄 요약
> Datadog 은 **자동화를 이미 3개 층에서 하고 있고**, 그 **사례를 계속 확장하는 중** 이다. 이 PoC 의 본질은 "Datadog 이 만든 걸 우리가 받아쓰는 검증" 이 아니라 **"우리 EC2 시나리오를 Datadog 확장 로드맵에 태우는 co-design 협의"**. 이 노트는 그 프레이밍을 명확히 잡는다.

---

## 1. "Preview" 의 실체

Datadog(및 SaaS 일반) 의 Preview 는 다음을 뜻한다:

1. **기능이 실제로 동작한다** — 데모/컨셉 이 아니라 초대받은 조직에서 프로덕션으로 굴러가는 상태
2. **접근이 제한적** — 특정 조직만 feature flag 로 활성화
3. **검증된 범위가 좁다** — 테스트된 특정 시나리오·환경만 지원
4. **SLA·가격·API 가 미확정** — GA 되면서 조정 가능

즉 **"되긴 되는데 되는 것들만 됨"**. 아무 시나리오나 자동으로 되는 만능이 아니라, Datadog 이 훈련·검증한 **특정 조합만** 처리한다.

이 성질이 이 PoC 의 모든 협의 지점을 설명한다:
- 왜 8/13 이 "실측" 이 아니라 "논의" 인가 → BIO 가 EC2 에서 Preview 도 안 열렸기 때문
- 왜 우리 6개 시나리오를 명시적으로 제출했는가 → 각 시나리오를 카탈로그에 태워야 하기 때문
- 왜 Workflow Automation 만 지금 실측 가능한가 → 이건 이미 GA 라 임의 시나리오 조합이 가능하기 때문

---

## 2. Datadog 은 이미 3개 층에서 자동화하고 있다

성숙도가 다른 3개 자동화 층이 동시에 움직이는 중.

| 층 | 자동화 방식 | 현재 상태 | 확장 방향 |
|---|---|---|---|
| **Workflow Automation** | 결정론적 룰 (사람이 짠 대본) | **GA**. 300+ 액션 카탈로그로 이미 운영 | **액션 카탈로그 확장** (통합 대상 서비스 증가) |
| **Bits Detection** | AI 감시 큐레이션 | **Preview**. APM HTTP/gRPC 서비스 대상 | **커버리지 확장** (다른 프로토콜, 다른 계층) |
| **BIO** | AI 조치 판단·제안 | **Preview**. Fargate + Serverless 특정 시나리오 | **환경 확장** (→ EC2) + **시나리오 확장** |

세 층 각각의 원래 목적과 배치 관계는 [[Datadog AI 자율운영 스택 — 3개 기능의 원래 목적]] 참조. 이 노트는 그 위에서 **"어떻게 확장되고 있는가"** 를 다룬다.

---

## 3. "사례 확장" 이 실제로 뜻하는 것

"자동화 사례를 늘린다" 는 세 방향으로 갈라진다.

### ① 환경 확장 (BIO 관점에서 가장 중요)
- 지금: Fargate, Serverless
- 로드맵: EC2, EKS 등
- **이번 PoC 의 주 협의 대상**

### ② 시나리오 확장
- AI 가 판단·조치할 수 있는 문제 유형이 늘어남
- 지금: Fargate 태스크 크래시, OOM 등 검증된 소수
- 늘어남: 좀비 프로세스, 메모리 트렌드, DB 커넥션 이슈, Slow Query 등

### ③ 액션 확장 (Workflow 관점)
- Workflow Automation 이 호출할 수 있는 외부 도구
- 지금: 300+ (AWS, Slack, PagerDuty, Jira, Terraform Cloud 등)
- 늘어남: 새 통합 서비스 추가

---

## 4. MegaZone 이 이 확장에서 하는 역할

"자동화 사례를 늘려나가고 싶다" 에서 **누가 어떤 사례를 내는지** 가 중요해진다. Datadog 은 두 종류의 입력을 필요로 한다:

1. **자체 발굴 시나리오** — 제품팀이 우선순위를 판단해 넣음
2. **고객이 실제 운영에서 낸 시나리오** — 진짜 페인이 있다는 증명

MegaZone MSP 가 제출한 6개 시나리오는 **2번** 을 채우는 재료다. 각 시나리오가 어떤 확장 방향과 매핑되는지:

| # | 시나리오 | 확장 방향 매핑 |
|---|---|---|
| 1 | 좀비/고아 프로세스 kill | ① 환경(EC2) + ② 시나리오(프로세스 정리) |
| 2 | 디스크 청소 | ① 환경 + ② 시나리오(파일시스템 정리) |
| 3 | 메모리 누수 사전 재시작 | ① 환경 + ② 시나리오(트렌드 판단 + 재시작 조치) |
| 4 | CPU Spike → ASG 스케일 | ① 환경 + ③ 액션(ASG 통합) — Workflow 로 지금도 가능 |
| 5 | DB Connection 누수 | ① 환경(EC2+RDS) + ② 시나리오(복합 신호 판단) |
| 6 | RDS Slow Query 스케일업 | ① 환경(RDS) + ② 시나리오(쿼리 프로파일 판단) |

각 시나리오가 어떤 조각들을 새로 필요로 하는지 명시적으로 드러난다. **8/13 미팅의 실제 대화** 는 이 매핑을 하나씩 짚으며 로드맵 우선순위를 협의하는 것.

---

## 5. 같은 협의를 양쪽이 다르게 본다

### Datadog 관점
- **EC2 확장 로드맵의 우선순위 시나리오 수집**
- **얼리 어답터 확보** — Preview 오픈 시 실측 파트너
- 대형 MSP 의 실운영 페인 = 다른 고객에게도 팔릴 시나리오 검증

### MegaZone 관점
- **자기 페인이 로드맵 상위에 오르도록 유도**
- **EC2 자율운영 얼리 액세스 획득** — Preview 오픈 시 먼저 사용
- MSP 서비스 차별화 — 자율운영 노하우 축적

양쪽이 win-win 구조. 흔한 벤더-고객 관계라기보다는 **co-design 파트너십** 성격이 강한 이유가 여기에 있다.

---

## 6. 정리

- Datadog 은 자동화를 **이미** 하고 있다 (3개 층, 각기 다른 성숙도)
- "Preview" 는 미완성이 아니라 **초대받은 조직만 쓸 수 있는 검증된 특정 시나리오**
- "사례 확장" 은 **환경 · 시나리오 · 액션** 세 축으로 갈라진다
- 이 PoC 의 본질은 **"우리 EC2 시나리오를 Datadog 확장 로드맵에 태우는 협의"**
- MegaZone 은 소비자가 아니라 **co-designer** 위치
- 8/13 미팅은 6개 시나리오를 확장 프레임(환경/시나리오/액션)에 매핑하며 우선순위를 협의하는 자리

---

## 관련 노트

- [[Datadog PoC — 진행 방향 및 Terraform 준비]] — PoC 전체 진행 방향과 인프라 개요
- [[Datadog AI 자율운영 스택 — 3개 기능의 원래 목적]] — 3개 자동화 층의 제품 정의
- [[메가존 PoC 실행 계획]] — 우리가 실행하는 실측 무대
